# WebLens API Contract

## Purpose

This document defines the initial API contract between the Chrome Extension and the WebLens backend.

The contract allows the extension and backend to be developed independently.

The API is versioned so that future changes can be introduced without breaking existing clients.

---

## Base URL

Development:

```text
http://localhost:8000
```
